# Standalone projects

These folders are **separate demos and coursework** that the main site links to. They are grouped here so the repository root stays easy to scan for the primary Vite + React portfolio under `src/`.
