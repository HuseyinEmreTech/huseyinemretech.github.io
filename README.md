# `public/`

Bu klasördeki dosyalar **Vite build sırasında olduğu gibi `dist/` köküne kopyalanır** (ör. `gis-project.html` → canlı sitede `/gis-project.html`). SPA girişi `index.html` repo kökünde kalır.
